on:
  push:
    branches:
    - master

jobs:
  hello_world_job:
    steps:
      - name: Say hello
        run: |
          echo "Hello World!"
      - name: Now it is
        run: |
          date
      - name: Directory content
        run: |
          ls -l
    runs-on: ubuntu-latest
      
