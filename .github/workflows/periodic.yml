name: Periodic health check

on:
  push:
    branches:
      - master

jobs:
  health_check:
    runs-on: ubuntu-latest
    steps:
      - name: Check the deployed service URL
        uses: jtalk/url-health-check-action@v3
        with:
          url: https://fullstack-open-pokedex-vdc9.onrender.com/
